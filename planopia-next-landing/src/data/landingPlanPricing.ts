/**
 * Cennik na landing — liczby pochodzą z server/constants/planCatalog.js przez generator
 * (npm run knowledge:build). Ten plik jest tylko re-eksportem, żeby istniejące importy nie zmieniały ścieżki.
 */
export * from './landingPlanPricing.generated'
